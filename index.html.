<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Rock Nations — Phones</title>
<style>
:root{--bg:#07090d;--card:#10141b;--card2:#151b24;--text:#f5f7fb;--muted:#aab3c2;--line:#252d39;--accent:#f4f7ff;--dark:#080a0e}
*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:linear-gradient(180deg,#06080c,#0b0f15);color:var(--text)}
header{position:sticky;top:0;z-index:10;background:rgba(7,9,13,.92);backdrop-filter:blur(12px);border-bottom:1px solid var(--line)}
.wrap{max-width:1180px;margin:auto;padding:0 18px}.nav{min-height:72px;display:flex;align-items:center;justify-content:space-between;gap:12px}
.logo{font-size:22px;font-weight:900;letter-spacing:.5px}.logo span{color:#9ca8ba}
.nav-actions{display:flex;gap:8px}.btn{border:1px solid var(--line);background:var(--card2);color:var(--text);padding:10px 14px;border-radius:10px;font-weight:700;cursor:pointer;text-decoration:none;display:inline-flex;align-items:center;justify-content:center}
.btn.primary{background:var(--accent);color:var(--dark);border-color:var(--accent)}.btn.danger{background:#2a1114;border-color:#5d2329;color:#ffb9c0}
.hero{padding:54px 0 28px;text-align:center}.hero h1{font-size:clamp(38px,7vw,68px);margin:0 0 12px;line-height:1}.hero p{max-width:700px;margin:0 auto;color:var(--muted);font-size:17px}
.toolbar{display:flex;justify-content:space-between;align-items:center;gap:12px;margin:20px 0;flex-wrap:wrap}.toolbar input{background:var(--card);border:1px solid var(--line);color:var(--text);padding:12px 14px;border-radius:10px;min-width:230px}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(245px,1fr));gap:18px;padding-bottom:55px}
.card{background:linear-gradient(180deg,#11161e,#0d1117);border:1px solid var(--line);border-radius:18px;overflow:hidden;box-shadow:0 10px 35px rgba(0,0,0,.22)}
.photo{width:100%;aspect-ratio:1/1;object-fit:cover;background:#0b0e13;display:block}.info{padding:16px}.name{font-size:20px;font-weight:850;margin:0 0 6px}.storage{color:var(--muted);font-size:14px}.price{font-size:24px;font-weight:900;margin:12px 0}.about{color:#c1c8d4;font-size:14px;line-height:1.5;min-height:42px;white-space:pre-wrap}
.card-actions{display:flex;gap:8px;margin-top:15px}.card-actions .btn{flex:1}
section.panel{background:#0d1219;border-top:1px solid var(--line);padding:40px 0}.panel h2{margin-top:0}.contact{color:var(--muted)}.contact a{color:var(--text)}
#admin{display:none}.admin-box{background:#0d1219;border:1px solid var(--line);border-radius:16px;padding:18px;margin-bottom:30px}.form-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:12px}.field label{display:block;color:var(--muted);font-size:12px;margin-bottom:6px}.field input,.field textarea{width:100%;background:#080b10;border:1px solid var(--line);border-radius:9px;color:var(--text);padding:10px}.field textarea{min-height:95px;resize:vertical}.full{grid-column:1/-1}.admin-row{display:grid;grid-template-columns:70px 1fr 110px 110px 1fr auto;gap:8px;align-items:center;border-top:1px solid var(--line);padding:12px 0}.admin-row input{width:100%;background:#080b10;border:1px solid var(--line);color:var(--text);border-radius:8px;padding:9px}.small{font-size:12px;color:var(--muted)}footer{border-top:1px solid var(--line);padding:25px 0;color:var(--muted);text-align:center}
@media(max-width:650px){.admin-row{grid-template-columns:1fr}.admin-row img{width:70px}.nav{min-height:62px}.hero{padding-top:35px}.toolbar input{width:100%}}
</style>
</head>
<body>
<header><div class="wrap nav"><div class="logo">ROCK <span>NATIONS</span></div><div class="nav-actions"><a class="btn" href="#contact">Contact</a><button class="btn primary" onclick="toggleAdmin()">Edit Store</button></div></div></header>
<main>
<div class="wrap">
<section class="hero"><h1>Rock Nations</h1><p>Premium phones. Simple ordering. Prices and product details can be edited from the store editor.</p></section>
<div class="toolbar"><h2 style="margin:0">Phones</h2><input id="search" placeholder="Search phones..." oninput="render()"></div>
<div id="products" class="grid"></div>
</div>

<section id="admin" class="panel"><div class="wrap">
<h2>Store Editor</h2>
<div class="admin-box">
<p class="small">Changes are saved in this browser. You can edit names, storage, prices, descriptions and payment links. Use “Add phone” to create another product.</p>
<div id="adminList"></div>
<div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:15px">
<button class="btn primary" onclick="addProduct()">+ Add phone</button>
<button class="btn" onclick="exportCatalog()">Export catalog</button>
<button class="btn" onclick="resetCatalog()">Reset demo catalog</button>
</div>
</div>
</div></section>

<section id="contact" class="panel"><div class="wrap">
<h2>Contact</h2>
<div class="contact">For orders and questions: <a href="mailto:brightbella3@gmail.com">brightbella3@gmail.com</a></div>
<p class="small">Payment links shown on product cards are the Binance Pay request links supplied for this catalog. Verify the amount before paying.</p>
</div></section>
</main>
<footer>© <span id="year"></span> Rock Nations</footer>

<script>
const DEFAULT_PRODUCTS = [
  {
    "id": 1,
    "name": "iPhone 15 Pro Max",
    "storage": "256GB",
    "price": 2200,
    "currency": "USDT",
    "image": "phone-1.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453806100949639168&billType=request_a_payment"
  },
  {
    "id": 2,
    "name": "iPhone 15 Pro",
    "storage": "256GB",
    "price": 1600,
    "currency": "USDT",
    "image": "phone-2.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453805785083232256&billType=request_a_payment"
  },
  {
    "id": 3,
    "name": "iPhone 15 Plus",
    "storage": "128GB",
    "price": 1200,
    "currency": "USDT",
    "image": "phone-3.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453805684371750912&billType=request_a_payment"
  },
  {
    "id": 4,
    "name": "iPhone 15",
    "storage": "128GB",
    "price": 900,
    "currency": "USDT",
    "image": "phone-4.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453804668493889536&billType=request_a_payment"
  },
  {
    "id": 5,
    "name": "iPhone 14 Pro Max",
    "storage": "512GB",
    "price": 750,
    "currency": "USDT",
    "image": "phone-5.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453805095907188736&billType=request_a_payment"
  },
  {
    "id": 6,
    "name": "iPhone 14 Pro",
    "storage": "256GB",
    "price": 600,
    "currency": "USDT",
    "image": "phone-6.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453806326384918528&billType=request_a_payment"
  },
  {
    "id": 7,
    "name": "iPhone 14 Plus",
    "storage": "128GB",
    "price": 550,
    "currency": "USDT",
    "image": "phone-7.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453806246652755968&billType=request_a_payment"
  },
  {
    "id": 8,
    "name": "iPhone 14",
    "storage": "128GB",
    "price": 400,
    "currency": "USDT",
    "image": "phone-8.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453806012631318528&billType=request_a_payment"
  },
  {
    "id": 9,
    "name": "Premium iPhone",
    "storage": "512GB",
    "price": 280,
    "currency": "USDT",
    "image": "phone-9.jpg",
    "about": "",
    "paymentLink": "https://app.binance.com/uni-qr/request-to-pay?billOrderId=453805899108016128&billType=request_a_payment"
  }
];
let products = JSON.parse(localStorage.getItem("rockNationsProducts") || "null") || DEFAULT_PRODUCTS;
function save(){localStorage.setItem("rockNationsProducts",JSON.stringify(products));render();renderAdmin();}
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));}
function render(){
 const q=document.getElementById("search").value.toLowerCase();
 const list=products.filter(p=>(p.name+" "+p.storage+" "+p.about).toLowerCase().includes(q));
 document.getElementById("products").innerHTML=list.map(p=>`
 <article class="card">
  ${p.image?`<img class="photo" src="images/${esc(p.image)}" alt="${esc(p.name)}">`:`<div class="photo"></div>`}
  <div class="info">
   <div class="name">${esc(p.name||"Phone name")}</div>
   <div class="storage">${esc(p.storage||"Storage / model")}</div>
   <div class="price">${esc(p.price)} ${esc(p.currency||"USDT")}</div>
   <div class="about">${esc(p.about||"About this phone — add your description in Edit Store.")}</div>
   <div class="card-actions">
    ${p.paymentLink?`<a class="btn primary" href="${esc(p.paymentLink)}" target="_blank" rel="noopener">Pay with Binance</a>`:""}
    <button class="btn" onclick="focusEdit(${p.id})">Details</button>
   </div>
  </div>
 </article>`).join("") || `<p class="contact">No phones found.</p>`;
}
function renderAdmin(){
 document.getElementById("adminList").innerHTML=products.map((p,i)=>`
 <div class="admin-row" data-id="${p.id}">
  <img src="${p.image?`images/${esc(p.image)}`:""}" style="width:60px;height:60px;object-fit:cover;border-radius:8px;background:#080b10">
  <input value="${esc(p.name)}" placeholder="Phone name" onchange="upd(${p.id},'name',this.value)">
  <input value="${esc(p.storage)}" placeholder="Storage" onchange="upd(${p.id},'storage',this.value)">
  <input type="number" value="${esc(p.price)}" placeholder="Price" onchange="upd(${p.id},'price',this.value)">
  <input value="${esc(p.paymentLink)}" placeholder="Binance payment link" onchange="upd(${p.id},'paymentLink',this.value)">
  <button class="btn danger" onclick="removeProduct(${p.id})">Delete</button>
  <div class="field full"><label>About this phone</label><textarea onchange="upd(${p.id},'about',this.value)" placeholder="Write your phone description here...">${esc(p.about)}</textarea></div>
  <div class="small full">Photo: ${esc(p.image||"No photo")} — To change a photo, replace the matching image file in the images folder and keep the same filename.</div>
 </div>`).join("");
}
function upd(id,key,value){const p=products.find(x=>x.id===id); if(p){p[key]=key==="price"?Number(value):value;save();}}
function addProduct(){
 const id=Date.now(); products.push({id,name:"New Phone",storage:"",price:0,currency:"USDT",image:"",about:"",paymentLink:""});save();
 document.getElementById("admin").scrollIntoView({behavior:"smooth"});
}
function removeProduct(id){if(confirm("Delete this phone?")){products=products.filter(p=>p.id!==id);save();}}
function resetCatalog(){if(confirm("Reset the catalog to the demo products?")){products=DEFAULT_PRODUCTS.map(p=>({...p}));save();}}
function toggleAdmin(){const a=document.getElementById("admin");a.style.display=a.style.display==="block"?"none":"block";if(a.style.display==="block")a.scrollIntoView({behavior:"smooth"});}
function focusEdit(id){document.getElementById("admin").style.display="block";document.getElementById("admin").scrollIntoView({behavior:"smooth"});}
function exportCatalog(){
 const blob=new Blob([JSON.stringify(products,null,2)],{type:"application/json"});
 const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="rock-nations-catalog.json";a.click();URL.revokeObjectURL(a.href);
}
document.getElementById("year").textContent=new Date().getFullYear();
render();renderAdmin();
</script>
</body>
</html>